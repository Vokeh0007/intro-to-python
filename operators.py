#types
    #arithmetic -- +, -, *, /, %, **, //
    #comparison -- ==, !=, >, <, >=, <=
    #logical -- and, or, not
    #assignment -- =, +=, -=, *=, /=, %=, **=, //=, &=, |=, ^=, >>=, <<=
    #identity -- is, is not
    #bitwise -- &, |, ^, ~, <<, >>
    #special -- in, not in, is, is not

